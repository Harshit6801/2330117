{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that captures the full lifecycle of application events",
  "main": "dist/logger.js",
  "types": "dist/logger.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/logger.ts"
  },
  "keywords": ["logging", "middleware", "typescript", "observable"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}
